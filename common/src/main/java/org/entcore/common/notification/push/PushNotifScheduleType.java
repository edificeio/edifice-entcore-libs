package org.entcore.common.notification.push;

/** Values of the postgres {@code schedule_type} enum backing the {@code scheduled} column. */
public enum PushNotifScheduleType {
    IMMEDIATE, AT_DATE
}
